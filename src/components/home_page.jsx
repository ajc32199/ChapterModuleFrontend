import React, { useState } from 'react';

const HomePage = () => {
    return(
        <div>
            <h1>Welcome to the TKE Chapter Module</h1>
            <p>Use the navigation bar to access the different sections of the site</p>
        </div>
    );
};

export default HomePage;